{
  "actorOutputSchemaVersion": 1,
  "title": "Ashby Job Scraper Output",
  "description": "Job postings scraped from Ashby job boards",
  "properties": {
    "jobs": {
      "type": "string",
      "title": "Job Postings",
      "description": "Scraped job postings from Ashby job boards",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    }
  }
}
